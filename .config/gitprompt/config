# vim:ft=sh

if [ ! -f "$HOME/src/github/system-repos/bash-git-prompt/gitprompt.sh" ]; then
    return
fi

GIT_PROMPT_ONLY_IN_REPO=1

# Avoid fetching remote status
# GIT_PROMPT_FETCH_REMOTE_STATUS=0

# Avoid searching for changed files in submodules
GIT_PROMPT_IGNORE_SUBMODULES=0

# Avoid setting virtual environment infos for node/python/conda environments
# GIT_PROMPT_WITH_VIRTUAL_ENV=0


# Show upstream tracking branch
# GIT_PROMPT_SHOW_UPSTREAM=1

# determines counting of untracked files: can be no, normal or all
# GIT_PROMPT_SHOW_UNTRACKED_FILES=normal


# Avoid printing the number of changed files
# GIT_PROMPT_SHOW_CHANGED_FILES_COUNT=0


# GIT_PROMPT_START=...    # uncomment for custom prompt start sequence
# GIT_PROMPT_END=...      # uncomment for custom prompt end sequence


# as last entry source the gitprompt script
GIT_PROMPT_THEME=Custom # use custom theme specified in file GIT_PROMPT_THEME_FILE (default ~/.git-prompt-colors.sh)
GIT_PROMPT_THEME_FILE=~/.config/gitprompt/themes/primary.sh


# Set theme
# GIT_PROMPT_THEME=Default_Ubuntu
# GIT_PROMPT_THEME=Single_line_Ubuntu


source $HOME/src/github/system-repos/bash-git-prompt/gitprompt.sh
